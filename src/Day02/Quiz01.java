package Day02;

public class Quiz01 {
    public static void main(String[] args) {
        int pay = 567890;
/*

        */
        /*System.out.println("\n\\n만원 : " + (pay / 1000)
            + "장\n천원 : " + (pay / 1000) % 1000
            + "장\n백원 : " + (pay / 100) % 100
            + "개\n십원 : " + (pay / 10) % 10 ;
                /*
        int man, chun, back, sip; // 몪 구하기 변수
        int m_na, ch_na, b_na; // 나머지 구하기 변수
        int pay = 567890; // 변수 초기화

        man = pay / 10000;
        m_na = pay % 10000;
        chun = m_na / 1000;
        ch_na = m_na % 1000;
        back = ch_na / 100;
        b_na = ch_na % 100;
        sip = b_na / 10;
*/

    }
}
